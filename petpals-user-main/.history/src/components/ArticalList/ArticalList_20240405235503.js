import "./style.scss";
import { ArticalItem } from "./ArticalItem";

const listArtical = [];

export const ArticalList = ({ number }) => {
  return (
    <div>
      <ArticalItem />
    </div>
  );
};
